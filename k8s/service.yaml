apiVersion: v1
kind: Service
metadata:
  name: hoya-service
  labels:
    app: hoya
spec:
  selector:
    app: hoya
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
